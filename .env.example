# =============================================================================
# Rental Commerce - Environment Variables
# =============================================================================
# 이 파일을 .env로 복사한 후 필요한 값을 수정하세요.
#   cp .env.example .env
#
# 주의: .env 파일은 .gitignore에 포함되어 있어 커밋되지 않습니다.
# =============================================================================

# ---------------------------------------------------------------------------
# Spring Boot
# ---------------------------------------------------------------------------
# 활성 프로파일 (local | dev | staging | prod)
SPRING_PROFILES_ACTIVE=local

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
# 호스트에서 접근할 앱 포트
APP_PORT=8080

# ---------------------------------------------------------------------------
# MySQL
# ---------------------------------------------------------------------------
# 호스트에서 접근할 MySQL 포트
MYSQL_PORT=3306
# root 비밀번호 (로컬 개발용)
DB_ROOT_PASSWORD=root
# 앱 전용 사용자
DB_USERNAME=rental
# 앱 전용 비밀번호 (로컬 개발용, 실제 배포 시 반드시 변경)
DB_PASSWORD=rental

# ---------------------------------------------------------------------------
# Redis
# ---------------------------------------------------------------------------
# 호스트에서 접근할 Redis 포트
REDIS_PORT=6379

# ---------------------------------------------------------------------------
# MinIO (S3 호환 오브젝트 스토리지)
# ---------------------------------------------------------------------------
# API 포트
MINIO_API_PORT=9000
# 콘솔 UI 포트
MINIO_CONSOLE_PORT=9001
# 접근 키 (로컬 개발용)
MINIO_ACCESS_KEY=minioadmin
# 시크릿 키 (로컬 개발용, 실제 배포 시 반드시 변경)
MINIO_SECRET_KEY=minioadmin

# ---------------------------------------------------------------------------
# JWT
# ---------------------------------------------------------------------------
# JWT 서명 키 (로컬 개발용, 실제 배포 시 반드시 변경)
JWT_SECRET=local-dev-jwt-secret-change-in-production
