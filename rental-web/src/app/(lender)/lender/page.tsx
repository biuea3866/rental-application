import { LenderDashboard } from "@/components/lender/LenderDashboard";

export const metadata = {
  title: "내 대시보드 — Rental Commerce",
};

export default function LenderHomePage() {
  return <LenderDashboard />;
}
